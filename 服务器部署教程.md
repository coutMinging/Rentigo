# 服务器部署教程

本文档面向**运维/部署人员**，目标是把「厂房及公寓楼出租管理系统」从代码部署到一台可对外访问的服务器上。

按本文从头执行，预计耗时 **30~60 分钟**（不含域名备案的等待时间）。

---

## 目录

- [一、部署前准备](#一部署前准备)
- [二、部署架构](#二部署架构)
- [三、部署方式选择](#三部署方式选择)
- [四、详细部署步骤（IP 访问）](#四详细部署步骤ip-访问)
- [五、绑定域名与启用 HTTPS](#五绑定域名与启用-https)
- [六、数据备份与恢复](#六数据备份与恢复)
- [七、日常运维](#七日常运维)
- [八、首次上线：切换到真实数据](#八首次上线切换到真实数据)
- [九、常见问题排查](#九常见问题排查)
- [十、安全清单](#十安全清单)
- [附录 A：环境变量说明](#附录-a环境变量说明)
- [附录 B：部署文件说明](#附录-b部署文件说明)
- [附录 C：Windows 内网部署（不用云服务器）](#附录-cwindows-内网部署不用云服务器)

---

## 一、部署前准备

### 1.1 服务器要求

| 项目 | 要求 | 说明 |
| --- | --- | --- |
| CPU / 内存 | 2 核 2G 起 | 够用。1 核 1G 打包前端会 OOM |
| 磁盘 | 40G 起 | 系统 + 依赖约 2G，其余留给数据和备份 |
| 系统 | Ubuntu 22.04 / 24.04、Debian 12 | `deploy/install.sh` 的自动化部分只适配这两个 |
| Node.js | ≥ 18，推荐 20 | 无需预装，脚本会自动装 |
| 数据库 | **无需安装** | 使用 SQLite 嵌入式数据库，由程序直接读写文件 |

> **关于内存**：`npm run build` 打包前端比较吃内存，2G 机器建议先加一块 swap，否则可能被 OOM Killer 杀掉。
>
> ```bash
> # 加 2G swap（2G 内存的机器强烈建议执行）
> fallocate -l 2G /swapfile && chmod 600 /swapfile
> mkswap /swapfile && swapon /swapfile
> echo '/swapfile none swap sw 0 0' >> /etc/fstab
> free -h   # 确认已生效
> ```

### 1.2 需要准备的信息

- [ ] 服务器公网 IP
- [ ] 服务器 SSH 登录方式（密码或密钥）
- [ ] 代码仓库地址（Git）
- [ ] 域名（可选，见 [第五节](#五绑定域名与启用-https)）
- [ ] 一个强密码，用于登录系统管理员账号

### 1.3 部署前建议：先在本机验证

在动服务器之前，先在自己电脑上跑一遍生产模式，确认这套代码本身是好的：

```bash
npm install
cp server/.env.example server/.env      # Windows: copy server\.env.example server\.env
# 编辑 server/.env，给 JWT_SECRET 填一串 32 位以上的随机值
npm start
```

浏览器打开 <http://localhost:3001>，能出登录页就说明代码没问题，可以上服务器了。

---

## 二、部署架构

**整台服务器只需要一个 Node 进程**，前端页面和 API 都由它提供：

```
                        ┌─────────────────────────────────────┐
   浏览器  ───────────►  │  nginx  :80 / :443                  │
   （PC 端）            │  （可选，用于域名与 HTTPS）          │
                        └──────────────┬──────────────────────┘
                                       │ 反向代理
                                       ▼
                        ┌─────────────────────────────────────┐
                        │  Node  :3001                        │
                        │  ├─ /api/**      → 后端接口         │
                        │  ├─ /uploads/**  → 上传的图片/附件  │
                        │  └─ 其余路径      → web/dist 前端   │
                        └──────────────┬──────────────────────┘
                                       │
                        ┌──────────────▼──────────────────────┐
                        │  server/data/                       │
                        │  ├─ app.db        ← 全部业务数据    │
                        │  └─ uploads/      ← 上传的附件      │
                        └─────────────────────────────────────┘
```

关键点：

- **不需要装 MySQL / Redis**，SQLite 是单文件数据库，跟着程序走。
- **不需要单独部署前端**，后端会托管 `web/dist`（可用 `SERVE_WEB=false` 关闭）。
- 前端的定时任务（逾期/到期状态流转）跑在同一个进程里，每 30 分钟执行一次。
- **`server/data/` 目录就是全部身家**，务必单独备份，更新代码时不要覆盖或删除。

---

## 三、部署方式选择

| 场景 | 需要域名 | 需要备案 | 怎么做 |
| --- | --- | --- | --- |
| 内网测试、先跑起来看看 | 不需要 | 不需要 | 只做 [第四节](#四详细部署步骤ip-访问)，用 `IP:3001` 访问 |
| 长期对外使用，正式环境 | 需要 | 大陆服务器**必须**备案 | 第四节 + [第五节](#五绑定域名与启用-https) |

> **备案提醒**：服务器在中国大陆时，用域名对外提供服务必须先完成 ICP 备案（免费，通常 1~3 周），否则 80/443 端口会被拦截。
> 想跳过备案就用香港/新加坡节点，代价是访问延迟略高。

---

## 四、详细部署步骤（IP 访问）

### 第 1 步：连接服务器

```bash
ssh root@你的服务器IP
```

如果创建服务器时用的是普通用户（如 `ubuntu`），后续需要 `sudo` 的命令都已标注。

### 第 2 步：更新系统并安装基础工具

```bash
apt update && apt upgrade -y
apt install -y git curl
```

### 第 3 步：获取代码

**安装目录约定为 `/opt/farental`**，脚本默认也认这个路径。

```bash
sudo mkdir -p /opt/farental
sudo chown "$USER" /opt/farental          # 让当前用户拥有目录，方便以后 git pull
git clone <你的仓库地址> /opt/farental
cd /opt/farental
```

> 如果没有 git 仓库，也可以用 `scp` / `rsync` 把本地代码传上去，只要最终目录是 `/opt/farental` 即可。

### 第 4 步：一键安装

```bash
# 先做语法检查（建议不要跳过）
bash -n deploy/install.sh

# 执行安装（需要 root）
sudo bash deploy/install.sh
```

脚本会依次完成：

1. 检查并安装 Node.js 20（已装则跳过）
2. 创建无登录权限的系统账号 `farental`，服务以它运行（不裸奔 root）
3. 安装依赖（`npm install`，含构建期工具）
4. 生成 `server/.env`，其中 **`JWT_SECRET` 和管理员密码都是随机生成的**
5. 构建前端产物到 `web/dist`
6. 把 `server/data` 与 `.env` 的归属改为 `farental`
7. 安装并启动 systemd 服务
8. 打印访问地址和初始管理员密码

安装成功时最后会打印类似内容，**请立即记下初始密码**：

```
[install] 安装完成，服务已启动

  访问地址   http://<服务器公网IP>:3001
  账号       admin
  初始密码   XXXXXXXXXXXXXXX   ← 请立即登录后修改
```

> 脚本是**幂等**的，重复执行不会覆盖已有的 `server/.env`。以后更新代码也可以直接重跑。

### 第 5 步：放行端口

到**云厂商控制台 → 安全组 / 防火墙**，放行入方向：

| 端口 | 协议 | 何时需要 |
| --- | --- | --- |
| 3001 | TCP | 只用 IP 访问时 |
| 80 | TCP | 配域名访问时 |
| 443 | TCP | 启用 HTTPS 时 |

如果服务器本机开了 ufw：

```bash
ufw allow 3001/tcp      # 只用 IP 访问
# 或
ufw allow 80/tcp && ufw allow 443/tcp
```

> **重要**：配了 nginx 之后，请把 3001 从公网安全组里**移除**，只保留 80/443，让流量统一走 nginx。

### 第 6 步：验证

```bash
# 服务状态应为 active (running)
systemctl status farental

# 健康检查，应返回 {"code":0,...}
curl http://127.0.0.1:3001/api/health

# 看启动日志
journalctl -u farental -n 50 --no-pager
```

浏览器打开 `http://<服务器公网IP>:3001`，看到登录页即部署成功。

### 第 7 步：登录并改密码

用打印出的 `admin` + 初始密码登录，**立即在系统内修改密码**。

---

## 五、绑定域名与启用 HTTPS

> 大陆服务器：先完成域名备案，再执行本节。

### 5.1 域名解析

在域名服务商处添加一条 A 记录，指向服务器公网 IP：

| 记录类型 | 主机记录 | 记录值 |
| --- | --- | --- |
| A | `rental`（或你想要的子域名） | 你的服务器公网 IP |

等待解析生效（通常几分钟）：

```bash
ping rental.example.com     # 能解析到你的 IP 即可
```

### 5.2 安装并配置 nginx

```bash
apt install -y nginx

# 放入反向代理配置，并把 server_name 改成你的域名
cp /opt/farental/deploy/nginx.conf /etc/nginx/conf.d/farental.conf
sed -i 's/server_name _;/server_name rental.example.com;/' /etc/nginx/conf.d/farental.conf

nginx -t                    # 必须先通过语法检查
systemctl reload nginx
```

此时用域名访问 `http://rental.example.com` 应该已经能打开系统。

### 5.3 签发 HTTPS 证书

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d rental.example.com
```

- certbot 会自动申请证书、改写 nginx 配置、并配置 HTTP 自动跳转 HTTPS。
- 证书**自动续期**由 certbot 自带的 systemd timer 负责，无需自己加 crontab。
- 验证续期能力：`certbot renew --dry-run`

完成后系统地址为 `https://rental.example.com`。

### 5.4 收尾

1. 确认云安全组里 **3001 已关闭**，只留 80/443。
2. 在 `server/.env` 里确认 `CORS_ORIGINS` 保持为空（同源部署不需要跨域）。

---

## 六、数据备份与恢复

**这一节是整篇文档最重要的部分。** `server/data/` 里的东西丢了就没了。

### 6.1 数据在哪

```
server/data/
├── app.db              ← 全部业务数据（厂房、公寓、租客、租约、账单、流水）
├── app.db-wal          ← SQLite 预写日志（运行中会有）
├── app.db-shm          ← SQLite 共享内存（运行中会有）
├── uploads/            ← 上传的图片、证件、附件
└── backups/            ← 备份快照输出目录（本脚本生成）
```

> **不要直接复制 `app.db` 当备份**。数据库运行在 WAL 模式下，最新写入可能还在 `app.db-wal` 里，单独拷主库会丢数据。

### 6.2 手动备份

```bash
cd /opt/farental
npm run backup
```

输出示例：

```
[backup] 数据库快照 → /opt/farental/server/data/backups/app-20260917-162822.db（0.33 MB）
[backup] 附件目录 → /opt/farental/server/data/backups/uploads-20260917-162822
[backup] 完成，当前保留 14 份，目录：/opt/farental/server/data/backups
```

脚本用 SQLite 的 `VACUUM INTO` 生成**事务一致的快照**，服务运行中执行也不会影响使用。

### 6.3 自动备份（建议必配）

```bash
crontab -e
```

加入一行（每天凌晨 3 点备份）：

```cron
0 3 * * * cd /opt/farental && /usr/bin/npm run backup -w server >> /var/log/farental-backup.log 2>&1
```

### 6.4 备份到另一块盘 / 异地（强烈建议）

默认备份和数据库在同一块盘上，**磁盘故障时一起丢**。建议把备份写到别处：

```bash
# 备份到挂载的另一块盘
cd /opt/farental && npx tsx server/src/scripts/backup.ts /mnt/backup

# 再用 rsync 同步到异地/NAS
rsync -av /opt/farental/server/data/backups/ user@backup-host:/backup/farental/
```

保留份数可通过环境变量调整（默认 14 份）：

```bash
BACKUP_KEEP=30 npm run backup -w server
```

### 6.5 恢复步骤

> **顺序很重要**：恢复前必须先停服务，并且要**删掉旧的 `-wal` / `-shm` 文件**，否则残留的 WAL 会和新库文件冲突，导致数据损坏。

```bash
# 1) 停服务
sudo systemctl stop farental

# 2) 先给当前数据留个后路（万一恢复错了还能退回来）
cd /opt/farental/server/data
cp -a app.db app.db.before-restore

# 3) 用快照覆盖数据库，并清掉旧的 WAL / SHM
cp backups/app-20260917-162822.db app.db
rm -f app.db-wal app.db-shm

# 4) 如需要，恢复附件
rm -rf uploads && cp -a backups/uploads-20260917-162822 uploads

# 5) 确认属主正确并启动
sudo chown -R farental:farental /opt/farental/server/data
sudo systemctl start farental

# 6) 验证
curl http://127.0.0.1:3001/api/health
journalctl -u farental -n 30 --no-pager
```

> **建议每半年做一次恢复演练**（可以在一台测试机上做）。只备份不演练，等于没有备份。

---

## 七、日常运维

### 7.1 常用命令速查

| 操作 | 命令 |
| --- | --- |
| 查看服务状态 | `systemctl status farental` |
| 启动 / 停止 / 重启 | `systemctl start / stop / restart farental` |
| 实时看日志 | `journalctl -u farental -f` |
| 看最近 50 行日志 | `journalctl -u farental -n 50 --no-pager` |
| 查看端口占用 | `ss -lntp \| grep 3001` |
| 手动备份 | `cd /opt/farental && npm run backup` |
| 健康检查 | `curl http://127.0.0.1:3001/api/health` |

### 7.2 更新代码

```bash
cd /opt/farental

# 1) 更新前先备份（很重要）
npm run backup

# 2) 拉取新代码
git pull

# 3) 装依赖 + 重新构建前端
npm install
npm run build

# 4) 重启服务
sudo systemctl restart farental

# 5) 验证
sleep 3 && curl http://127.0.0.1:3001/api/health
```

**注意事项**：

- 不要执行 `git clean -fdx`，会删掉被忽略的 `server/data/`；
- 不要删除或覆盖 `server/data/`；
- 代码里如果新增了环境变量，记得同步到 `server/.env`（对照 `server/.env.example` 检查）。

### 7.3 定时任务说明

系统内置的定时任务（账单逾期流转、租约到期预警、站内消息生成）随服务启动，每 **30 分钟**执行一次，**不需要额外配置 cron**。

到期预警天数在「系统设置 → 基础参数」里调整。

---

## 八、首次上线：切换到真实数据

演示环境下系统自带一批演示数据（6 处厂房、24 间公寓、11 位租客、8 份租约、122 期账单）。正式使用前需要清掉。

### 8.1 清空演示数据

```bash
# 1) 停服务
sudo systemctl stop farental

# 2) 备份当前演示库（万一还想拿它当演示素材）
cd /opt/farental && npm run backup

# 3) 删除数据库文件
sudo rm -f server/data/app.db server/data/app.db-wal server/data/app.db-shm

# 4) 启动服务，会自动重建空库并初始化管理员
sudo systemctl start farental

# 5) 从日志中获取新生成的管理员密码
journalctl -u farental -n 20 --no-pager | grep -E '管理员|密码'
```

会看到：

```
[db] 演示数据已关闭，仅初始化角色、基础参数与管理员账号
[db] 管理员账号已创建：admin
[db] 随机初始密码（仅显示这一次，请登录后立即修改）：xxxxxxxxxxxx
```

> **密码只在日志里出现一次**，找不到就再清库重启一次。
>
> 想跳过"随机密码"这一步，先在 `server/.env` 里填好 `ADMIN_PASSWORD`，再执行清库，重启后就是你自己设的密码。

### 8.2 想找回演示数据

把 `server/.env` 里的 `SEED_DEMO` 改成 `true`，清库重启即可重新生成演示数据。启动时会打印一条警告，属正常现象。

### 8.3 上线前检查清单

- [ ] `server/.env` 里的 `JWT_SECRET` 是随机值，且**已单独记录保存**
- [ ] `NODE_ENV=production`、`SEED_DEMO=false`
- [ ] 管理员密码已改成强密码
- [ ] 演示数据已清空
- [ ] 自动备份已配置（`crontab -l` 能看到）
- [ ] 至少做过一次恢复演练
- [ ] HTTPS 已启用（有域名时）
- [ ] 云安全组未对公网开放 3001（配了 nginx 时）
- [ ] `server/data/` 不参与任何形式的代码清理

---

## 九、常见问题排查

### 服务起不来

```bash
journalctl -u farental -n 100 --no-pager
```

常见原因：

| 日志内容 | 原因 | 解决 |
| --- | --- | --- |
| `生产环境必须配置 JWT_SECRET` | `.env` 里密钥为空 | 填一串 32 位以上随机值后重启 |
| `EADDRINUSE` | 3001 被占用 | `ss -lntp \| grep 3001` 找到进程后处理 |
| `Cannot find module 'tsx'` | 依赖用了 `--omit=dev` 安装 | 重新执行 `npm install`（不要加 `--omit=dev`） |
| `not find web/dist` | 前端未构建 | `cd /opt/farental && npm run build` |
| 构建时进程被 Killed | 内存不足 | 加 swap（见 [1.1](#11-服务器要求)） |

### 页面能打开，但接口全部失败

```bash
curl http://127.0.0.1:3001/api/health       # 后端是否存活
```

- 后端正常 → 检查 nginx 配置与 `nginx -t` 输出
- 后端挂掉 → 看 `journalctl -u farental`

### 刷新页面出现 404

不该发生——刷新时的路由兜底由 Node 负责（非 `/api`、`/uploads`、`/assets` 的 GET 都会回落到 `index.html`）。

如果确实出现，说明请求没走到 Node，检查 nginx 是否把请求转到了别的服务，或 `deploy/nginx.conf` 是否被改动。

### 上传大文件失败

默认单文件上限 **10MB**，该值目前**写死在代码里**（`server/src/config/index.ts` 的 `uploadMaxSize`），不能用环境变量覆盖。

要放宽，需要改两处（两处必须同步，否则 nginx 会先一步拦截）：

1. `server/src/config/index.ts` → `uploadMaxSize`，例如改为 `50 * 1024 * 1024`
2. `deploy/nginx.conf` → `client_max_body_size`，例如改为 `52m`

改完重启生效：

```bash
sudo systemctl restart farental
```

> 后端由 tsx 直接运行 TypeScript，改完不需要重新构建。

### 时区不对 / 账单日期偏差

```bash
timedatectl set-timezone Asia/Shanghai
sudo systemctl restart farental
```

### 数据看板数字与实际不符

系统的金额核算以 `server/src/utils/billing.ts` 的 `buildBillPlan()` 为唯一真源，账单在租约创建时一次性落库。如果怀疑数据异常，先对比某份租约的账单明细，而不是重算。

---

## 十、安全清单

上线后建议逐条确认：

- [ ] **`JWT_SECRET` 使用随机值**，且不重复用于其它系统
- [ ] **不用 root 运行服务**（`install.sh` 已默认创建 `farental` 账号）
- [ ] **管理员密码为强密码**，不使用 `123456`
- [ ] **只暴露必要端口**，配了 nginx 就关闭公网 3001
- [ ] **启用 HTTPS**，避免 token 明文传输
- [ ] **`.env` 权限 600**，不提交到版本库（`.gitignore` 已包含）
- [ ] **服务器 SSH 使用密钥登录**，禁用密码登录
- [ ] **备份到异地**，并定期演练恢复
- [ ] **及时更新系统补丁**（`apt upgrade`）
- [ ] **按角色分配账号**，财务/运维使用各自账号，不共用 `admin`

---

## 附录 A：环境变量说明

配置文件路径：`/opt/farental/server/.env`（模板见 `server/.env.example`）

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `NODE_ENV` | — | 生产环境必须为 `production`，会关闭演示数据与跨域并启用启动自检 |
| `PORT` | `3001` | 后端监听端口 |
| `JWT_SECRET` | — | **生产必填**，随机值，长度 ≥ 16。更换后所有已登录用户会被强制下线 |
| `SEED_DEMO` | 生产 `false` | 是否写入演示数据，仅对**空库**生效 |
| `ADMIN_USERNAME` | `admin` | 空库初始化时的管理员账号名 |
| `ADMIN_PASSWORD` | 空 | 留空则随机生成并打印在日志中；仅空库初始化时使用 |
| `SERVE_WEB` | 自动 | 是否由后端托管 `web/dist`，留空＝存在产物即托管 |
| `WEB_DIST_DIR` | `web/dist` | 前端产物目录，一般不用改 |
| `CORS_ORIGINS` | 空 | 跨域白名单，逗号分隔。同源部署留空即可 |
| `LEASE_WARN_DAYS` | `30` | 租约到期预警提前天数，也可在系统设置里改 |
| `BACKUP_KEEP` | `14` | 备份保留份数（备份脚本使用） |

**修改环境变量的流程**：

```bash
sudo nano /opt/farental/server/.env
sudo systemctl restart farental
journalctl -u farental -n 20 --no-pager   # 确认启动正常
```

---

## 附录 B：部署文件说明

```
deploy/
├── install.sh          一键安装脚本（Debian/Ubuntu）
├── farental.service    systemd 服务模板，install.sh 会安装到 /etc/systemd/system/
└── nginx.conf          nginx 反向代理配置，需手动复制到 /etc/nginx/conf.d/

server/src/scripts/backup.ts    备份脚本（通过 npm run backup 调用）
server/.env.example             环境变量模板
```

对应的 npm 脚本：

| 命令 | 作用 |
| --- | --- |
| `npm start` | 构建前端 + 以生产模式启动后端（本机/容器场景） |
| `npm run backup` | 执行一次备份 |
| `npm run build` | 只构建前端 |
| `npm run seed` | 手动写入种子数据（仅空库生效） |
| `npm run dev` | 开发模式（前端 5173 + 后端 3001，两个进程） |

> ⚠️ **`install.sh` 尚未在真实 Linux 服务器上完整跑过验证**。首次执行前请务必先 `bash -n deploy/install.sh` 做语法检查；执行过程中若报错，把 `journalctl -u farental -n 100 --no-pager` 的输出保留下来以便排查。
>
> 如果 `bash -n` 报 `$'\r': command not found`，说明文件换行符是 CRLF，执行 `sed -i 's/\r$//' deploy/*.sh` 修正。仓库已通过 `.gitattributes` 强制这些文件使用 LF，正常 clone 不会出现该问题。

---

## 附录 C：Windows 内网部署（不用云服务器）

如果使用者都集中在同一个园区/办公室，一台常开的 Windows 电脑就够了，**不需要买服务器、不需要域名、不需要备案**。

### C.1 准备

- 找一台常开的电脑（建议 8G 内存以上）
- 安装 Node.js 20：<https://nodejs.org/>（选 LTS 版）

### C.2 部署

```powershell
# 1) 拉代码
cd D:\
git clone <你的仓库地址> farental
cd D:\farental

# 2) 装依赖
npm install

# 3) 配置环境变量
copy server\.env.example server\.env
# 用记事本打开 server\.env，给 JWT_SECRET 填一串 32 位以上的随机值

# 4) 启动
npm start
```

### C.3 让同事访问

```powershell
ipconfig        # 找到内网 IP，例如 192.168.1.50
```

同事用浏览器访问 `http://192.168.1.50:3001` 即可。

如果访问不了，在【Windows 防火墙 → 高级设置 → 入站规则】里为 TCP 3001 端口添加一条允许规则。

### C.4 开机自动启动

用「任务计划程序」创建任务：

1. 触发器：**计算机启动时**
2. 操作：启动程序
   - 程序：`C:\Program Files\nodejs\node.exe`
   - 参数：`--import tsx src/index.ts`
   - 起始位置：`D:\farental\server`
3. 勾选「不管用户是否登录都要运行」
4. 设置里勾选「如果任务失败，按以下频率重新启动」

### C.5 数据备份

```powershell
# 手动
cd D:\farental
npm run backup

# 用任务计划程序每天执行一次，或做个批处理放到 D:\farental\backup.bat
```

备份文件在 `D:\farental\server\data\backups\`，建议定期复制到移动硬盘或网盘。

### C.6 内网方案的限制

- 外网（出差、用手机流量）**访问不了**，只能在内网用；
- 电脑关机/休眠期间系统不可用；
- 没有 HTTPS，不适合在不可信网络中使用。

需要外网访问时，最简单的办法是用 Cloudflare Tunnel 或 frp 做内网穿透，再把方案升级到正式服务器部署。
